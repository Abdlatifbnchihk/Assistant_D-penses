## 1. Fix Blade View Variable Names

- [x] 1.1 In `show.blade.php`: change `$receipt->depenses` to `$receipt->expenses` and `$depense` to `$expense`
- [x] 1.2 In `index.blade.php`: change `$recu->depenses->count()` to `$recu->expenses->count()`
